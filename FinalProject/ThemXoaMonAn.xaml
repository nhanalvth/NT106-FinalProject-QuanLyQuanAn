<Window x:Class="FinalProject.ThemXoaMonAn"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FinalProject"
        mc:Ignorable="d"
        Title="ThemXoaMonAn" Height="450" Width="800">
    <Grid Height="434" VerticalAlignment="Bottom">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />

        </Grid.RowDefinitions>
        <StackPanel Grid.Column="0" Margin="16">
            
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="16">
                <Button x:Name="bth_Them"
            Content="_Thêm"
            Margin="0,0,10,0"
            Click="btn_Them_Click"
            BorderThickness="0"
            IsEnabled="{Binding DataContext.ControlsEnabled, RelativeSource={RelativeSource AncestorType={x:Type Window}, Mode=FindAncestor}}"
            Style="{StaticResource MaterialDesignRaisedDarkButton}"
            ToolTip="Resource name: MaterialDesignRaisedLightDarkButton" Width="133" Background="#FF107679"/>
                <Button x:Name="btn_Sua"
            Content="_Sửa"
            Margin="0,0,10,0"
            BorderThickness="0"
            IsEnabled="{Binding DataContext.ControlsEnabled, RelativeSource={RelativeSource AncestorType={x:Type Window}, Mode=FindAncestor}}"
            Style="{StaticResource MaterialDesignRaisedDarkButton}"
            ToolTip="Resource name: MaterialDesignRaisedLightDarkButton"  Width="133" Background="#FF107679"/>
                <Button x:Name="btn_Xoa"
            Content="_Xóa"
            Margin="0,0,10,0"
            Click="btn_Xoa_Click"
            BorderThickness="0"
            IsEnabled="{Binding DataContext.ControlsEnabled, RelativeSource={RelativeSource AncestorType={x:Type Window}, Mode=FindAncestor}}"
            Style="{StaticResource MaterialDesignRaisedDarkButton}"
            ToolTip="Resource name: MaterialDesignRaisedLightDarkButton"  Width="133" Background="#FF107679"/>
                <Button x:Name="btn_Save"
            Content="_Lưu"
            Click="btn_Save_Click"
            BorderThickness="0"
            IsEnabled="{Binding DataContext.ControlsEnabled, RelativeSource={RelativeSource AncestorType={x:Type Window}, Mode=FindAncestor}}"
            Style="{StaticResource MaterialDesignRaisedDarkButton}"
            ToolTip="Resource name: MaterialDesignRaisedLightDarkButton"  Width="133" Background="#FF107679"/>
            
            </StackPanel>

            <StackPanel Margin="20">
                <!-- THÊM MÓN -->
                <TextBlock Text="Thêm món mới" FontWeight="Bold"/>
                <ComboBox x:Name="cbAddName" Width="300"/>
                <ComboBox x:Name="cbAddImage" Width="300"/>
                <ComboBox x:Name="cbAddRating" Width="300"/>

                <!-- SỬA MÓN -->
                <TextBlock Text="Sửa món" FontWeight="Bold" Margin="10,5,0,0"/>
                <ComboBox x:Name="cbOldName" Width="300"/>
                <ComboBox x:Name="cbNewName" Width="300"/>
                <ComboBox x:Name="cbOldImage" Width="300"/>
                <ComboBox x:Name="cbNewImage" Width="300"/>
                <ComboBox x:Name="cbOldRating" Width="300"/>
                <ComboBox x:Name="cbNewRating" Width="300"/>

                <!-- XÓA MÓN -->
                <TextBlock Text="Xóa món" FontWeight="Bold" Margin="10,5,0,0"/>
                <ComboBox x:Name="cbDeleteName" Width="300"/>

                <!-- BUTTONS -->
                
            </StackPanel>

        </StackPanel>
    </Grid>
</Window>
